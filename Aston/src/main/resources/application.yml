spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/user_service
    username: app_user
    password: secret
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  application:
    name: user-service

server:
  port: 8080
